import {
  CORE_PERMISSIONS,
  DEFAULT_ROLE_DEFINITIONS,
} from '../permissions/core-permissions';

export const REGISTRATION_CODE_TTL_MINUTES = 10;

export const DEFAULT_PERMISSIONS = CORE_PERMISSIONS;

export { DEFAULT_ROLE_DEFINITIONS };
